# Patches

Patches are sourced from the radxa-pkg/aic8800 repository:
https://github.com/radxa-pkg/aic8800/tree/main/debian/patches

To populate this directory, run `download-patches.sh` from the repository root.

Only SDIO-relevant and kernel compatibility patches are included (OpenWrt 25.12.x / kernel 6.12).
